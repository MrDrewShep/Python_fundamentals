From linux:

    Start a session
        >> tmux
        Fire up venv
            >> source venv/bin/activate
        Fire up python app
            >> python scheduler.py
        Exit from session
            >> ctrl + b   >> d

    List sessions
        >> tmux ls
        
    Attach into a session (edit)
        >> tmux attach